title_selectors = ["//h2[contains(@class, 'title')]"]
body_selectors = ["//div[contains(@class, 'content-body')]"]
date_selectors = ["//div[contains(@class, 'submitted')]"]
author_selectors = []
strip_selectors = [
  "//em[contains(text(), 'Listen to the latest ')]",
  "//img[@alt='Listen on Apple Podcasts']//parent::a//parent::p",
]
strip_id_or_class = []
strip_image_src = []
native_ad_selectors = []
tidy = false
prune = false
autodetect_on_failure = true
single_page_link_selectors = []
next_page_link_selectors = []
replace_strings = []

[http_headers]

[[tests]]
  URL = "https://www.schwab.com/resource-center/insights/content/schwab-market-update"
  Contains = []
