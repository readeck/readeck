title_selectors = ["//div[contains(@class, \"post\")]/h2"]
body_selectors = ["//div[contains(@class, \"post\")]"]
date_selectors = ["//div[contains(@class, \"post\")]/p[1]"]
author_selectors = ["//div[contains(@class, \"post\")]/p[position()=last()]/text()[1]"]
strip_selectors = [
  "//div[contains(@class, \"post\")]/h2[1]",
  "//div[contains(@class, \"post\")]/p[1]",
  "//div[contains(@class, \"post\")]/p[position()=last()]",
]
strip_id_or_class = []
strip_image_src = []
native_ad_selectors = []
tidy = false
prune = false
autodetect_on_failure = true
single_page_link_selectors = []
next_page_link_selectors = []
replace_strings = []

[http_headers]

[[tests]]
  URL = "http://www.symmetrymagazine.org/breaking/?p=12784"
  Contains = []
