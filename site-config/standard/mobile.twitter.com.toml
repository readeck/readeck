title_selectors = ["concat(\"Tweet from \", (//*[contains(@class, 'UserNames-displayName') or contains(@class, 'fullname')])[1])"]
body_selectors = ["(//div[contains(@class, 'TweetDetail-text') or contains(@class, 'tweet-text')])[1]"]
date_selectors = ["(//div[contains(@class, 'TweetDetail-timeAndGeo') or contains(@class, 'metadata')])[1]"]
author_selectors = ["(//*[contains(@class, 'UserNames-displayName') or contains(@class, 'fullname')])[1]"]
strip_selectors = []
strip_id_or_class = []
strip_image_src = []
native_ad_selectors = []
tidy = true
prune = false
autodetect_on_failure = true
single_page_link_selectors = []
next_page_link_selectors = []
replace_strings = []

[http_headers]

[[tests]]
  URL = "https://mobile.twitter.com/medialens/status/216883678582804480"
  Contains = ["is all but alone in challenging the tsunami of UK"]
