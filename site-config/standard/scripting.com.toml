title_selectors = ["//h1"]
body_selectors = ["//*[@class='storyPageText']/.."]
date_selectors = ["substring-before(substring-after(//*[@class='storyByline'], 'on'), 'at')"]
author_selectors = ["string('Dave Winer')"]
strip_selectors = [
  "//a[starts-with(@href, '#')]",
  "//*[@class='storyByline']",
]
strip_id_or_class = []
strip_image_src = []
native_ad_selectors = []
tidy = false
prune = false
autodetect_on_failure = true
single_page_link_selectors = []
next_page_link_selectors = []
replace_strings = []

[http_headers]

[[tests]]
  URL = "http://scripting.com/stories/2011/07/08/yeahImStillYawning.html"
  Contains = []
